# Local stack: Qdrant + the assistant.
#
#   docker compose up -d --build --wait          # qdrant + HTTP wrapper on :8080
#   docker compose run --rm ingest               # (re)index the corpus into Qdrant
#   docker compose run --rm chat                 # interactive CLI in the terminal
#   echo '{"session_id":"a","message":"hi"}' | docker compose run --rm -T chat chat --jsonl
#
# The assistant reads OPENROUTER_API_KEY from .env (copy .env.example). Without a key,
# set LLM_PROVIDER=mock in .env to run the no-credentials mode.
services:
  qdrant:
    image: qdrant/qdrant:v1.19.1
    restart: unless-stopped
    ports:
      - "${QDRANT_HTTP_PORT:-6333}:6333"
    volumes:
      - qdrant_data:/qdrant/storage
    environment:
      QDRANT__TELEMETRY_DISABLED: "true"
    healthcheck:
      test: ["CMD", "bash", "-c", ":> /dev/tcp/127.0.0.1/6333"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s

  app: &app
    build: .
    image: orbitmesh-assistant:local
    restart: unless-stopped
    env_file: .env
    environment:
      QDRANT_URL: http://qdrant:6333
    volumes:
      - app_data:/data
    ports:
      - "${APP_PORT:-8080}:8080"
    depends_on:
      qdrant:
        condition: service_healthy
      ingest:
        condition: service_completed_successfully

  ingest:
    <<: *app
    restart: "no"
    ports: []
    depends_on:
      qdrant:
        condition: service_healthy
    command: ["ingest"]

  chat:
    <<: *app
    restart: "no"
    ports: []
    stdin_open: true
    tty: true
    depends_on:
      qdrant:
        condition: service_healthy
    command: ["chat"]
    profiles: ["cli"]

volumes:
  qdrant_data:
  app_data:
